{
  "name": "app-name",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "electron-icon-builder": "^2.0.1",
    "electron-packager": "^12.2.0"
  },
  "scripts": {
    "start": "electron .",
    "build-icon": "electron-icon-builder --input=./public/icon.png --output=public --flatten",
    "packager": "electron-packager . 测试打包 --out 测试打包 --arch=x64 --overwrite --ignore=node_modules --icon=./public/favicon.ico"
  },
  "devDependencies": {
    "electron": "^23.1.3",
    "electron-vite": "^1.0.29"
  }
}
